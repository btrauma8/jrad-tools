import React from 'react';
import '../css/_main.css';

// just a place to import css
// You don't even need this component in your app.
// just for building this library.

export const Init = ({ children }:any) => {
    return <>{ children }</>
}